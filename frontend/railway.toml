[build]
builder = "dockerfile"
dockerfilePath = "frontend/Dockerfile"

[build.dockerArguments]
VITE_API_URL = "${{VITE_API_URL}}"
CACHEBUST = "20260702"

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
